@model IEnumerable<CasaGaillard.Models.situacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lado_cuba_numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lado_cuba_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.situacion_lado_numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.situacion_lado_letra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lado_cuba_numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lado_cuba_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.situacion_lado_numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.situacion_lado_letra)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
