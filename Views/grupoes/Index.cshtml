@model IEnumerable<CasaGaillard.Models.grupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.compartimento.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cuba.cuba1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.situacion.lado_cuba_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_grupo.nombre_grupo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.compartimento.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cuba.cuba1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.situacion.lado_cuba_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_grupo.nombre_grupo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
