@model IEnumerable<CasaGaillard.Models.revision_vehiculo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_revision)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.detalles)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ejecutor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vehiculo.marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_revision.tipo_revision1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_revision)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.detalles)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ejecutor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vehiculo.marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_revision.tipo_revision1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
